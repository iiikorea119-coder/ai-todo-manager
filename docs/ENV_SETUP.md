# 환경 변수 설정 가이드

## 🔧 환경 변수 설정 개요

이 프로젝트는 다음 환경 변수가 필요합니다:
- **Supabase**: 회원가입/로그인 및 데이터 저장
- **Google Gemini AI**: AI 기반 자연어 할 일 파싱

## 📝 단계별 설정 방법

---

## 🟦 Supabase 설정

### 1. Supabase 프로젝트 생성

1. [Supabase](https://supabase.com) 접속
2. "New Project" 클릭
3. 프로젝트 정보 입력:
   - **Name**: `ai-todo-manager` (또는 원하는 이름)
   - **Database Password**: 안전한 비밀번호 생성
   - **Region**: `Northeast Asia (Seoul)` 선택 (한국 사용자용)
4. "Create new project" 클릭
5. 프로젝트 생성 완료까지 대기 (1-2분)

### 2. API 키 확인

1. Supabase 대시보드에서 생성한 프로젝트 선택
2. 왼쪽 메뉴에서 **⚙️ Settings** 클릭
3. **API** 메뉴 클릭
4. 다음 정보 복사:
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon public** 키 (Public API key)

### 3. 환경 변수 파일 생성

프로젝트 루트 디렉토리에 `.env.local` 파일을 생성:

```bash
# Windows (PowerShell)
New-Item -Path .env.local -ItemType File

# Mac/Linux
touch .env.local
```

### 4. 환경 변수 입력

`.env.local` 파일을 열고 다음 내용 입력:

```env
# Supabase 프로젝트 URL (2단계에서 복사한 Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# Supabase Anon/Public Key (2단계에서 복사한 anon public 키)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inh4eHh4eHh4eHh4eHh4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE2ODY3NTg0MjUsImV4cCI6MjAwMjMzNDQyNX0.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**⚠️ 주의사항:**
- `xxxxxxxxxxxxx` 부분을 실제 값으로 교체
- 따옴표 없이 입력
- `=` 앞뒤에 공백 없이 입력

---

## 🤖 Google Gemini AI 설정

AI 기반 자연어 할 일 파싱 기능을 사용하려면 Google Gemini API 키가 필요합니다.

### 1. Google AI Studio 접속

1. [Google AI Studio](https://aistudio.google.com/app/apikey) 접속
2. Google 계정으로 로그인

### 2. API 키 생성

1. "Get API key" 또는 "API 키 만들기" 버튼 클릭
2. "Create API key in new project" 선택 (또는 기존 프로젝트 선택)
3. API 키 복사 (예: `AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX`)

### 3. 환경 변수에 추가

`.env.local` 파일에 다음 내용 추가:

```env
# Google Gemini AI API Key
GOOGLE_GENERATIVE_AI_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

### 4. 전체 환경 변수 예시

최종 `.env.local` 파일은 다음과 같이 구성됩니다:

```env
# Supabase 설정
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Google Gemini AI 설정
GOOGLE_GENERATIVE_AI_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

**⚠️ 중요:**
- `GOOGLE_GENERATIVE_AI_API_KEY`에는 `NEXT_PUBLIC_` 접두사를 붙이지 **않습니다**
- 이 키는 서버 사이드에서만 사용되며, 클라이언트에 노출되지 않습니다

### 5. 개발 서버 재시작

환경 변수를 적용하려면 개발 서버를 재시작해야 합니다:

```bash
# 1. 현재 실행 중인 서버 중지 (Ctrl + C)

# 2. 서버 재시작
npm run dev
```

## ✅ 설정 확인

### Supabase 확인

#### 방법 1: 브라우저 콘솔 확인

1. 회원가입 페이지 접속: `http://localhost:3000/signup`
2. 브라우저 개발자 도구 열기 (F12)
3. Console 탭 확인
4. 다음 메시지 확인:

```
=== Supabase 환경 변수 확인 ===
NEXT_PUBLIC_SUPABASE_URL: https://xxxxx.supabase.co ✅
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ✅ 설정됨
===============================
```

#### 방법 2: 테스트 회원가입

1. 회원가입 페이지에서 정보 입력
2. "회원가입" 버튼 클릭
3. 에러 메시지 확인:
   - ✅ **성공**: "회원가입이 완료되었습니다..."
   - ❌ **환경 변수 오류**: "Supabase 설정이 필요합니다..."
   - ❌ **연결 오류**: "Supabase 서버에 연결할 수 없습니다..."

### Gemini AI 확인

#### AI 기능 테스트

1. 로그인 후 메인 페이지 접속
2. "새 할 일 추가" 버튼 클릭
3. AI 자연어 입력 영역에 텍스트 입력 (예: "내일 오후 3시까지 중요한 팀 회의 준비하기")
4. "AI로 생성" 버튼 클릭
5. 결과 확인:
   - ✅ **성공**: 폼이 자동으로 채워짐 (제목, 설명, 날짜, 우선순위, 카테고리)
   - ❌ **API 키 오류**: "AI 서비스 설정이 완료되지 않았습니다"
   - ❌ **파싱 오류**: "AI 파싱 중 오류가 발생했습니다"

## 🐛 문제 해결

### Supabase 관련 문제

#### "Supabase 설정이 필요합니다"

**원인**: 환경 변수가 설정되지 않음

**해결 방법**:
1. `.env.local` 파일이 프로젝트 루트에 있는지 확인
2. 파일 이름이 정확한지 확인 (`.env.local`, 대소문자 구분)
3. 환경 변수 이름이 정확한지 확인
   - ✅ `NEXT_PUBLIC_SUPABASE_URL`
   - ❌ `SUPABASE_URL` (NEXT_PUBLIC_ 접두사 필수)
4. 개발 서버 재시작

#### "Supabase 서버에 연결할 수 없습니다"

**원인**: URL이나 키가 잘못됨

**해결 방법**:
1. Supabase 대시보드에서 URL과 키를 다시 복사
2. `.env.local` 파일의 값 확인
3. 값에 공백이나 따옴표가 없는지 확인
4. 개발 서버 재시작

#### "이미 가입된 이메일입니다"

**원인**: 해당 이메일로 이미 가입함

**해결 방법**:
1. 다른 이메일 주소 사용
2. 또는 로그인 페이지에서 로그인 시도
3. 또는 Supabase 대시보드에서 기존 사용자 삭제
   - Authentication > Users > 사용자 삭제

#### 개발 서버가 환경 변수를 인식하지 못함

**해결 방법**:
1. `.env.local` 파일 저장 확인
2. 개발 서버 완전히 종료 (Ctrl + C)
3. 터미널 재시작
4. 개발 서버 재시작

```bash
# PowerShell
npm run dev

# 또는 완전히 새로 시작
npx next dev
```

### Gemini AI 관련 문제

#### "AI 서비스 설정이 완료되지 않았습니다"

**원인**: Gemini API 키가 설정되지 않음

**해결 방법**:
1. `.env.local` 파일에 `GOOGLE_GENERATIVE_AI_API_KEY` 확인
2. API 키가 올바르게 입력되었는지 확인
3. `NEXT_PUBLIC_` 접두사가 **없는지** 확인 (서버 사이드 전용)
4. 개발 서버 재시작

#### "AI 파싱 중 오류가 발생했습니다"

**원인**: API 키가 잘못되었거나 만료됨

**해결 방법**:
1. [Google AI Studio](https://aistudio.google.com/app/apikey)에서 API 키 확인
2. 새 API 키 생성하여 교체
3. `.env.local` 파일 저장
4. 개발 서버 재시작

#### API 호출 제한 (Rate Limit) 오류

**원인**: Gemini API 무료 할당량 초과

**해결 방법**:
1. [Google AI Studio](https://aistudio.google.com/app/apikey) 대시보드에서 사용량 확인
2. 무료 플랜 제한:
   - gemini-2.0-flash-exp: 분당 15회 요청
   - 일일 요청 제한 확인
3. 필요시 유료 플랜 고려
4. 또는 잠시 후 다시 시도

#### AI가 날짜/시간을 잘못 파싱함

**원인**: 입력이 모호하거나 맥락이 부족함

**해결 방법**:
1. 더 구체적으로 입력 (예: "내일" 대신 "12월 27일")
2. 시간 형식 명확히 (예: "3시" 대신 "오후 3시")
3. 생성된 결과를 직접 수정
4. "AI로 다시 생성하기" 버튼으로 재시도

## 📁 파일 위치 확인

```
ai-todo-manager/
├── .env.local          ← 이 파일이 여기 있어야 함!
├── .gitignore          ← .env.local이 포함되어야 함
├── app/
├── components/
├── lib/
├── package.json
└── ...
```

## 🔒 보안 주의사항

### ❌ 절대 하지 말아야 할 것

1. **Git에 커밋하지 말 것**
   - `.env.local` 파일을 절대 Git에 추가하지 마세요
   - `.gitignore`에 포함되어 있는지 확인

2. **공개하지 말 것**
   - API 키를 온라인에 공유하지 마세요
   - 스크린샷이나 화면 공유 시 주의
   - GitHub, Discord 등에 키를 붙여넣지 마세요

3. **Service Role Key 노출하지 말 것**
   - Supabase `service_role` 키는 절대 클라이언트에 노출하지 마세요
   - `NEXT_PUBLIC_` 접두사를 붙이지 마세요

4. **Gemini API 키 보호**
   - `GOOGLE_GENERATIVE_AI_API_KEY`는 서버 사이드 전용
   - `NEXT_PUBLIC_` 접두사를 붙이면 클라이언트에 노출되어 위험함

### ✅ 해야 할 것

1. **`.gitignore` 확인**
   ```gitignore
   # .gitignore 파일에 포함되어야 함
   .env.local
   .env*.local
   ```

2. **프로덕션 환경 변수 별도 관리**
   - Vercel, Netlify 등 배포 플랫폼의 환경 변수 설정 사용

## 📚 추가 설정

### 데이터베이스 스키마 생성

환경 변수 설정 후 데이터베이스 테이블을 생성해야 합니다:

1. [Supabase 스키마 설정 가이드](./SUPABASE_SETUP.md) 참조
2. `supabase/schema.sql` 파일 실행

### 인증 설정

Supabase 대시보드에서 인증 설정 확인:

1. **Authentication** > **Providers** > **Email** 활성화
2. **Enable email confirmations** 설정 (선택):
   - ✅ 켜기: 이메일 인증 필요 (더 안전)
   - ❌ 끄기: 즉시 로그인 가능 (개발 편의)

## 🔗 관련 문서

- [Supabase 초기 설정](./SUPABASE_SETUP.md)
- [Supabase 공식 문서](https://supabase.com/docs)
- [Next.js 환경 변수](https://nextjs.org/docs/app/building-your-application/configuring/environment-variables)

---

**문제가 계속되면?**
- 브라우저 콘솔의 에러 메시지 확인
- Supabase 대시보드의 Logs 확인
- [Supabase Discord](https://discord.supabase.com) 커뮤니티 참여

